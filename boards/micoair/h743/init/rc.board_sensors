#!/bin/sh
# MicoAir H743 dual external raw IMU initialization

if ! icm42688p -S -b 1 -c 1 -R 0 start
then
	echo "ERROR [init] ICM42688P on M9/PD14 not detected"
fi

if ! icm42688p -S -b 1 -c 2 -R 0 start
then
	echo "ERROR [init] ICM42688P on M10/PD15 not detected"
fi
